part of '../_index.dart';

class SingleEcho extends StatelessWidget {
  const SingleEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
