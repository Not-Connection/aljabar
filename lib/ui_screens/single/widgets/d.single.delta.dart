part of '../_index.dart';

class SingleDelta extends StatelessWidget {
  const SingleDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
