part of '../_index.dart';

class BattleDelta extends StatelessWidget {
  const BattleDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
