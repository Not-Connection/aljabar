part of '../_index.dart';

class BattleEcho extends StatelessWidget {
  const BattleEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
